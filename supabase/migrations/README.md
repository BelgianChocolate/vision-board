# Supabase Setup

## Run migration
Copy the contents of `001_initial.sql` and run in Supabase SQL Editor.

## Create Storage bucket
Dashboard → Storage → New bucket → Name: `goal-images`, Public: YES

## Run storage policies
After creating the bucket, run the storage policy SQL from the bottom of `001_initial.sql` in the SQL Editor.
